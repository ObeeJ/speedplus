#!/bin/sh

echo "🛡️  [Pre-Commit Gate] Running monorepo lint & Go backend tests..."

# 1. Monorepo Linting Gate
pnpm turbo lint || exit 1

# 2. Go Backend Integration & Invariant Tests
(cd apps/api && DATABASE_URL="postgres://speedplus:speedplus@localhost:5433/speedplus?sslmode=disable" REDIS_URL="redis://localhost:6379" go test ./...) || exit 1

echo "✅ [Pre-Commit Gate] All checks passed cleanly!"
